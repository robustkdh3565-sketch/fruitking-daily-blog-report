# 네이버 블로그 주제·해시태그 연계 로직

## 조사 결론

- 네이버 블로그 공식 도움말은 상위 노출에 유리한 해시태그 개수를 공개하지 않는다.
- 네이버 클립은 해시태그를 최대 5개까지 허용하지만, 추천 노출은 해시태그 개수와 무관하게 콘텐츠 자체를 기준으로 한다고 안내한다. 클립의 5개 제한을 블로그 순위 공식으로 전용하지 않는다.
- 네이버 검색 원칙은 정확하고 유용한 정보, 검색 의도와 원본성을 강조하며, 숨김 키워드와 기계적으로 반복되는 패턴은 제한 대상이 될 수 있다.
- 상위 블로그 표본에서는 해시태그 수가 크게 달라 고정 개수 자체를 성과 원인으로 볼 수 없었다. 따라서 태그 수보다 제목·본문·태그의 의미 일치를 검증한다.

## 주제 최적화 순서

1. 당일 리서치에서 원문 ID·날짜·반응 수치가 확인된 후보만 사용한다.
2. 한 글이 해결할 검색 질문을 한 문장으로 정의한다.
3. 대표 키워드 한 개와 보조 키워드 2~4개를 정한다.
4. 제목 앞부분에 대표 키워드를 자연스럽게 사용한다.
5. 첫 300자에서 대표 질문에 답한다.
6. 소제목에 보조 질문을 배치하고 본문에서 실제로 해결한다.
7. 해시태그는 제목·본문·출처 데이터에 근거가 있는 단어만 생성한다.

## 해시태그 개수

기본 범위는 6~10개다. 이는 네이버의 비공개 랭킹 공식이 아니라 아래 역할을 빠짐없이 연결하면서 과도한 나열을 막기 위한 편집 범위다.

- 대표 주제: 1개
- 핵심 검색 질문·의도: 2개
- 행동·식단·원료·증상: 2~4개
- 방송·커뮤니티·인물·대상: 1~3개

역할을 채울 근거가 6개 미만이면 태그를 억지로 만들지 않고 원고의 주제 설계를 다시 검토한다. 10개를 초과하는 후보는 대표 검색 의도와 거리가 먼 순서로 제거한다.

## 태그 채택 조건

각 태그는 다음을 모두 통과해야 한다.

- 태그 근거 문구가 제목·본문·출처 데이터 중 하나에 실제로 존재
- 대표 검색 질문과 직접 연관
- 같은 뜻의 태그를 띄어쓰기만 바꿔 중복하지 않음
- 지나치게 넓은 `건강`, `일상`, `정보` 단독 태그를 사용하지 않음
- 원고에 없는 제품·효능·인물·증상을 태그로 추가하지 않음
- 인기 태그라는 이유만으로 글과 무관한 단어를 추가하지 않음

## 발행 후 검증

해시태그 개수를 성과 원인으로 미리 단정하지 않는다. 발행 1·3·7·14·30일 후 검색 유입 키워드와 순위를 기록하고, 30개 글 이상 쌓이면 태그 수보다 `대표 키워드 일치율`, `본문 근거율`, `검색 유입어와의 일치율`을 우선 분석한다.
