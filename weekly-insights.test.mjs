import assert from'node:assert/strict';import path from'node:path';import{buildWeeklyInsights,renderWeeklyInsights}from'./weekly-insights.mjs';const root=path.resolve('.'),result=buildWeeklyInsights('2026-08-30',root);assert.equal(result.availableDays,2);assert.equal(result.phase,'관찰 중');assert.ok(result.keywords.length>0);assert.ok(result.themes.length>0);const html=renderWeeklyInsights(result);assert.match(html,/현재 2\/7일/);assert.match(html,/관찰 키워드/);console.log(`weekly insights tests passed: ${result.keywords.length} keywords, ${result.themes.length} themes`);
