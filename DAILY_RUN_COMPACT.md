# 데일리 실행 런북

매일 실행할 때는 이 파일, 당일 `data/*.json`, 당일 `articles/*.md`만 먼저 읽는다. 검증 실패나 기준 충돌이 있을 때만 상세 규칙 문서를 연다.

## 입력

- 건강주제 리서치와 커뮤니티 리서치의 당일 자료
- 30일 이내 원문만 일일 주제 후보로 사용
- 원문 ID, 채널·프로그램명, 인물, 게시·방송일, URL, 공개된 조회·반응 수치
- 주제당 국내 공식 건강 근거 3개와 추천 이미지 5개

## 한 번에 처리하는 순서

1. 한국시간 당일의 커뮤니티·건강주제 리포트를 각각 확인한다. 하나가 없으면 해당 원천 작업을 한 번 재실행하고 다시 확인한다.
2. 한 원천만 복구되지 않아도 다른 당일 원천으로 계속 진행하고, 리포트에 `원천 일부 미수집`을 표시한다. 두 원천이 모두 없을 때만 발행을 중단하고 실패 원인을 알린다.
3. 후보를 한꺼번에 불러와 중복 주제를 제거한다. 과거 날짜의 후보·원고를 날짜만 바꿔 재사용하지 않는다.
4. `ISSUE_FIRST_SELECTION_RULES.md`로 전일 조회 증가속도·조회수·반응을 계산해 후보를 찾는다.
5. 후보 원문 본문, 핵심 이미지·영상, 연결 기사와 주요 댓글을 실제로 확인한다.
6. 줄거리 3~5문장, 확인 사실, 작성자 의견, 반복 반응과 확인 불가 항목을 구조화한다.
7. 원문 검증 14/20, 직접 연결성 12/15, 프룻킹 USP 3/5를 통과한 후보만 최대 3개 고른다. 1~2개만 통과해도 오류로 처리하지 않고 그대로 발행한다.
8. 각 주제의 3,000자 이상 완성 원고를 작성한다.
9. 제목·본문에 실제로 등장하는 근거로 해시태그 6~10개를 만든다.
10. 자연스러운 한국어 최종 보정을 한 번만 수행한다.
11. 인물·날짜·수치·URL·줄거리·건강 표현을 원본과 다시 대조한다.
12. 전체 검증을 한 번 실행하고 HTML을 한 번 생성한다.
13. GitHub push는 실패 시 원인을 확인하고 한 번 재시도한다. 커밋은 로컬에 보존하고 공개 URL이 HTTP 200인지 확인해야 발행 완료로 기록한다.
14. Gmail 발송은 실패 시 한 번 재시도한다. 그래도 실패하면 `data/outbox/YYYY-MM-DD.json`에 수신자·제목·공개 URL을 저장하고 다음 실행 시작 때 미발송 건부터 보낸다.
15. 이메일 제목과 본문에는 `프룻킹`이라는 단어를 사용하지 않는다. 메일명은 `데일리 블로그 리포트`로 통일하고 공개 리포트 URL만 안내한다.
16. 이메일의 일반 받는 사람(To)은 발신 계정으로 지정하고, 실제 수신자 `robustkdh3565@gmail.com`, `ownwellcorp@gmail.com`은 숨은참조(BCC)에만 넣어 서로의 주소가 보이지 않게 한다.
17. 모든 발송 메일에는 당일 리포트 URL과 누적 리포트 인덱스 URL을 각각 분리해 함께 넣는다.
18. 최초 생성 뒤 당일 두 원천 반영, 선정 부족 원인, 원문 사실, 제목·본문·해시태그·근거·이미지, AI 문체와 누적 분석을 자체 검토한다. 접근 가능한 공식 원문을 놓쳤다면 같은 실행에서 후보와 원고를 보완하고 한 번 다시 생성한다.
12. 발행 24시간 뒤 조회수와 검색 유입, 72시간 뒤 구매 클릭을 `record-publication-performance.mjs`로 저장한다. 접근할 수 없는 값은 0이 아니라 미수집으로 남기고 학습 표본에서 제외한다.
13. 커뮤니티 리서치와 건강주제 리서치의 최근 7일 리포트를 읽어 최하단 누적 키워드와 인접 확장 테마를 갱신한다. 7일 미만이면 누적 단계와 실제 보유 일수를 표시한다.

## 고정 출력

- 통과한 원고 최대 3개. 고정된 형식을 맞추려고 부적합 후보를 넣지 않음
- 제목 바로 아래 발행일
- 원문 URL, 조회수 또는 `미수집`, 게시·방송일
- 공식 자료 표기: `※ 참고한 국내 공식 자료:`
- 주제당 공식 근거 3개와 이미지 추천 5개
- 자연스러운 한국어 최종 보정 통과 표시
- 당일 HTML, 누적 인덱스, 공개 HTTPS URL

## 중단 조건

- 원문 ID·URL·날짜 불일치
- 원문 본문이나 핵심 첨부물을 끝까지 확인하지 못함
- 검증 줄거리 80자 미만 또는 제목만으로 내용 추정
- 원문 검증 14점 미만, 직접 연결성 12점 미만, USP 3점 미만, 총점 75점 미만
- 오래된 자료를 당일 자료처럼 사용
- 미수집 값을 0으로 대체
- 3,000자 미만, 근거 없는 해시태그, 공식 근거 3개 미만, 이미지 5개 미만
- em dash, AI 메타 문구, 기계적 순서 나열, 동일 종결어미 5회 이상 연속
- 확인되지 않은 건강 효능·인과관계·체험 창작

후보가 1~2개뿐인 것은 중단 조건이 아니다. 원천 하나가 미수집인 것도 중단 조건이 아니며, 사용한 원천과 미수집 원천을 투명하게 표시하고 계속 발행한다.

## 상세 규칙을 여는 경우

- 주제 선정 충돌: `RANKING_FIRST_RULES.md`
- 전일 이슈 점수: `ISSUE_FIRST_SELECTION_RULES.md`
- 본문 구성 판단: `NAVER_BLOG_WRITING_RULES.md`
- 건강 근거 판단: `HEALTH_EVIDENCE_RULES.md`
- 해시태그 판단: `TOPIC_HASHTAG_LOGIC.md`
- 절대 조건 확인: `ABSOLUTE_RULES.md`

이 구조의 목적은 규칙을 매번 전부 다시 읽지 않고, 실패한 부분의 상세 문서만 추가로 읽는 것이다.
